[package]
name = "RubyCoreLib"
version = "0.0.1"
edition = "2024"
description = "Assets for terminal games."
repositiory = "https://github.com/antiMRS/RubyCore"
authors = ["antiMRS"]

[dependencies]
num-traits = "0.2.19"

[target.'cfg(windows)'.dependencies]
minifb = "0.28.0"
font8x8 = "0.3.1"

[features]
default = ["colored", "terminal_color_rgb"]
colored = []
styled = []
terminal_color_cubes = []
terminal_color_rgb = []
terminal_color_legacy = []
